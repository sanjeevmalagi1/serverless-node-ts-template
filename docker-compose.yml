version: '3.8'

services:
  backend-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    networks:
      - backend-network

volumes:
  data:

networks:
  backend-network:
    driver: bridge
